export interface AircraftData {
    id: string;
    latitude: number;
    longitude: number;
    altitude: number;
    speed: number;
    heading: number;
    // Add more fields as needed
}

export interface RadarData {
    id: string;
    latitude: number;
    longitude: number;
    // Add more fields as needed
}